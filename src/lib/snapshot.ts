// Snapshot timestamp lives in its own module so client components can import it
// without pulling the full mock-flight dataset into the client bundle.
export const SNAPSHOT_TIMESTAMP = "2026-05-03T22:00:00.000Z";
